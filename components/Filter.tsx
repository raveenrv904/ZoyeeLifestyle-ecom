import React from "react";

const Filter = ({
  setProductPerPage,
}: {
  setProductPerPage: (value: number) => void;
}) => {
  return <section className="w-full bg-other">Filter</section>;
};

export default Filter;
